# 消融实验：Fluid 拆分 vs 跨 Phase 联合调度 贡献归因

## 目标

量化 U_ 类调度器的两个性能增益来源的**独立贡献**：
1. **Fluid 拆分**（wave 模型允许拆分 flow）
2. **跨 Phase 联合调度**（全局 ready-set 允许不同 phase 的 flow 同时竞争端口）

这是论文归档级实验，结果需要长期保存。

---

## 背景

当前 U_ 类算法 (N=8) 达到 ~43% improvement，D_ 类 ~19%。差距来自两个因素：
- U_ 允许把一个大 flow 拆成多个 wave 传输（fluid）
- U_ 允许 phase-0 和 phase-1 的 flow 在同一个 wave 中同时调度（跨 phase）

需要做 2×2 消融矩阵：

|  | 无跨 phase | 跨 phase |
|---|---|---|
| **无 fluid（atomic）** | D_atomic（当前 baseline ~19%） | U_atomic（待实现） |
| **有 fluid** | D_fluid（语义等价于 D_atomic，见下文） | U_fluid（当前 U_ ~43%） |

**关键洞察**：D_fluid ≈ D_atomic，因为 D_ 的 `_schedule_phase_orders` 按 phase 顺序处理，phase 内拆不拆 chunk 不影响总时间。所以实际上只需要测 3 个配置。

---

## 实现要求

### Task 1：给 `_run_global_matching_scheduler` 添加 `atomic` 参数

文件：`src/routesense/scheduler/multiphase_global.py`

在函数签名中新增参数：
```python
def _run_global_matching_scheduler(
    dispatch_matrix: list[list[int]],
    combine_matrix: list[list[int]],
    next_dispatch_matrix: list[list[int]],
    num_gpus: int,
    *,
    strategy: str,
    mode: str,
    prediction_confidence: float,
    expert_compute_delay: float,
    exact_matching: bool,
    wave_quantum: float | None,
    max_waves: int,
    residual_weight: float,
    barrier_weight: float,
    age_weight: float,
    prediction_weight: float,
    adaptive_prices: bool,
    price_step: float,
    price_decay: float,
    price_clip: float,
    iteration_budget: int,
    atomic: bool = False,  # ← 新增
) -> dict[str, Any]:
```

**核心修改**（约 L420 附近）：

当前 fluid 模式：
```python
duration = min(float(candidate["residual"]) for candidate in chosen)
```

atomic 模式改为：
```python
if atomic:
    # Atomic: 每个 flow 必须在一个 wave 中完整服务，不允许拆分
    # wave duration = max residual among chosen (每个 flow 全部传完)
    # 但不同 flow 的 residual 不同，取 max 意味着小 flow 会被过度服务
    # 正确做法：每个 flow 只服务自己的 residual，wave 内不同 flow 可以有不同的 duration
    # 但这样就不是统一的 wave 模型了
    # 
    # 实现方案：wave 按 flow 独立服务——每个 flow 在自己的 duration 内完成
    # 但同一个 wave 内的 flow 共享端口时间线
    # 所以 wave duration = max residual，小 flow 结束后端口不能复用（atomic 约束）
    duration = max(float(candidate["residual"]) for candidate in chosen)
else:
    duration = min(float(candidate["residual"]) for candidate in chosen)
```

**atomic 模式的 residual 更新逻辑**也需要修改：

```python
for candidate in chosen:
    flow_id = str(candidate["flow_id"])
    phase = int(candidate["phase"])
    src = int(candidate["src_gpu"])
    dst = int(candidate["dst_gpu"])
    if atomic:
        # Atomic: flow 完全服务，residual 直接归零
        served = float(residual[flow_id])
        residual[flow_id] = 0.0
        inbound_remaining[(phase, dst)] = max(0.0, inbound_remaining[(phase, dst)] - served)
        end = current_time + served
    else:
        # Fluid: 按 wave duration 部分服务
        residual[flow_id] = max(0.0, residual[flow_id] - duration)
        inbound_remaining[(phase, dst)] = max(0.0, inbound_remaining[(phase, dst)] - duration)
        end = current_time + duration
    
    barrier_done[(phase, dst)] = max(barrier_done[(phase, dst)], end)
    if phase < 2 and inbound_remaining[(phase, dst)] <= 1e-9:
        release_time[(phase + 1, dst)] = barrier_done[(phase, dst)] + (
            expert_compute_delay if phase == 0 else 0.0
        )
    schedule.append({
        "chunk_id": f"{flow_id}_wave{wave_count}",
        "flow_id": flow_id,
        "phase": phase,
        "size": float(served if atomic else duration),
        "served_volume": float(served if atomic else duration),
        "src": src,
        "dst": dst,
        "src_gpu": src,
        "dst_gpu": dst,
        "start": current_time,
        "end": end,
        "priority": [
            float(candidate["score"]),
            float(candidate["barrier_urgency"]),
            float(candidate["age"]),
            float(candidate["prediction_bonus"]),
        ],
    })

if atomic:
    # Atomic 模式下 current_time 前进到 wave 中最晚结束的时间
    current_time = max(float(entry["end"]) for entry in schedule if entry["chunk_id"].endswith(f"_wave{wave_count}"))
else:
    current_time += duration
wave_count += 1
```

**重要**：atomic 模式下，同一 wave 内不同 flow 可能有不同的 end time（因为 residual 不同），但它们的 start time 相同。replay validator 需要能通过——因为端口约束是 per-flow 检查的，不要求同一 wave 的所有 flow 同时结束。

### Task 2：注册 U_atomic 策略变体

文件：`src/routesense/scheduler/multiphase_global.py`

为 4 个 U_ 调度器各添加一个 `_atomic` 版本，复用相同的评分参数，但传入 `atomic=True`：

```python
def fast_schedule_u_gated_maxweight_matching_atomic(dispatch_matrix, combine_matrix, next_dispatch_matrix, num_gpus, *, model="full_duplex", expert_compute_delay=0.0, mode=EXECUTION_WINDOW_MODE, prediction_confidence=1.0, wave_quantum=None, max_waves=256):
    del model
    return _run_global_matching_scheduler(
        dispatch_matrix, combine_matrix, next_dispatch_matrix, num_gpus,
        strategy="O_gated_maxweight_matching_atomic",
        mode=mode, prediction_confidence=prediction_confidence,
        expert_compute_delay=expert_compute_delay,
        exact_matching=True, wave_quantum=wave_quantum, max_waves=max_waves,
        residual_weight=1.0, barrier_weight=1.0, age_weight=0.15, prediction_weight=0.50,
        adaptive_prices=False, price_step=0.0, price_decay=0.0, price_clip=0.0, iteration_budget=1,
        atomic=True,
    )
```

类似地为 `barrier_criticality_global_matching` 和 `gated_greedy_maximal` 创建 atomic 版本（参数与对应的 fluid 版本一致，只加 `atomic=True`）。

文件：`src/routesense/scheduler/strategies.py` 注册这些新策略。

文件：`src/routesense/evaluation/analysis.py` 的 `FAST_ALGORITHMS` 列表中添加 atomic 变体。

### Task 3：运行消融实验

创建实验脚本 `experiments/poc_line1/exp_ablation_fluid_vs_joint.py`：

```python
#!/usr/bin/env python3
"""消融实验：量化 fluid 拆分 vs 跨 phase 联合调度的独立贡献"""
import argparse, json, time
from pathlib import Path
from routesense.scheduler import (
    fast_schedule_birkhoff,
    fast_schedule_u_gated_maxweight_matching,
    fast_schedule_u_barrier_criticality_global_matching,
    # ... atomic 版本
)
from routesense.scheduler.oracle import pairwise_oracle

# 对 N=8, N=16, N=30 三种规模各跑一次完整对比
# 输出 4 列对比表：B_birkhoff | U_atomic | U_fluid | Oracle
```

**实验配置**：
- N=8: 使用现有 OLMoE 的 sample=16 和 sample=64 数据
- N=16: 使用现有 sample=64 数据
- N=30: 使用现有 sample=64 数据
- mode=execution_window（所有 phase 真实流量）
- prediction_confidence=1.0

### Task 4：生成归档结果

输出路径：`artifacts/poc_line1/ablation_fluid_vs_joint/`

生成文件：
1. `table_ablation.md` — 核心对比表
2. `summary.json` — 完整数据（含 makespan, latency, improvement, wave_count）
3. `analysis_notes.md` — 结论摘要

表格式：

```markdown
## Ablation: Fluid vs Joint Scheduling (N=8, sample=64)

| Algorithm | Fluid | Joint | Makespan | Improvement | Latency (ms) | E2E |
|-----------|-------|-------|----------|-------------|--------------|-----|
| B_birkhoff (D_atomic) | ❌ | ❌ | ... | ...% | ...ms | ...% |
| O_gated_maxweight_atomic (U_atomic) | ❌ | ✅ | ... | ...% | ...ms | ...% |
| O_gated_maxweight (U_fluid) | ✅ | ✅ | ... | ...% | ...ms | ...% |
| oracle_perfect | ❌ | ✅ | ... | ...% | ...ms | ...% |

### 贡献归因
- Joint scheduling 贡献: (U_atomic - D_atomic) / (U_fluid - D_atomic) × 100%
- Fluid splitting 贡献: (U_fluid - U_atomic) / (U_fluid - D_atomic) × 100%
```

---

## 验证要求

1. **replay validator 通过**：U_atomic 的 schedule 必须通过 `replay_and_audit_schedule` 验证
   - atomic 模式下同一个 wave 内不同 flow 可能有不同 end time，validator 按 per-flow 区间检查端口冲突，不要求同 wave 统一 end
2. **makespan 单调性**：`U_atomic ≥ U_fluid`（atomic 不允许拆分，所以不会比 fluid 更好）
3. **D_fluid 等价性验证**：可选地实现 D_fluid（把 chunk 拆成 unit token 后喂给 `_schedule_phase_orders`），验证 makespan 与 D_atomic 一致

---

## 关于 D_fluid 的说明

D_fluid 语义上等价于 D_atomic：`_schedule_phase_orders` 按 phase 顺序处理 chunk，phase 内的总传输时间 = sum(所有 chunk 的有效时间)，与 chunk 粒度无关。拆不拆 chunk 不改变 phase 内的调度结果，也不改变 phase 间的 barrier 时间。

因此 **D_fluid 不需要实现**。如果为了论文严谨性需要验证这一点，可以临时实现并确认 makespan 一致，然后移除。

---

## 关于 U_atomic 语义的精确说明

U_atomic 模式：
- ✅ 跨 phase 联合调度（全局 ready-set，不同 phase 的 flow 可以同时在同一个 wave 中）
- ❌ 不允许 fluid 拆分（每个 flow 在一个 wave 中完整传输，不可分割）

这样 U_atomic 与 Oracle 的语义一致（都是 atomic chunk + 跨 phase），可以做公平对比。

预期结果：
- `U_atomic ≈ Oracle` 或 `U_atomic ≤ Oracle`（Oracle 是 atomic 最优解）
- `U_fluid > U_atomic`（fluid 提供额外增益）
- `U_fluid > Oracle`（fluid 解空间更大，不是 bug）

---

## 执行命令

```bash
cd RS

# 1. 运行消融实验（N=8）
python experiments/poc_line1/exp_ablation_fluid_vs_joint.py \
    --scale 8 --samples 64 \
    --output artifacts/poc_line1/ablation_fluid_vs_joint/n8_s64/

# 2. 运行消融实验（N=16）
python experiments/poc_line1/exp_ablation_fluid_vs_joint.py \
    --scale 16 --samples 64 \
    --output artifacts/poc_line1/ablation_fluid_vs_joint/n16_s64/

# 3. 运行消融实验（N=30）
python experiments/poc_line1/exp_ablation_fluid_vs_joint.py \
    --scale 30 --samples 64 \
    --output artifacts/poc_line1/ablation_fluid_vs_joint/n30_s64/

# 4. 汇总结果
python experiments/poc_line1/exp_ablation_fluid_vs_joint.py \
    --summary artifacts/poc_line1/ablation_fluid_vs_joint/
```

---

## 验收标准

- [ ] U_atomic 通过 replay validator
- [ ] U_atomic makespan ≥ U_fluid makespan（原子化不会更好）
- [ ] U_atomic 与 Oracle 的 gap 缩小（相比 U_fluid vs Oracle）
- [ ] 三个规模的表格都生成
- [ ] summary.json 包含完整的贡献归因百分比
- [ ] 所有结果文件在 `artifacts/poc_line1/ablation_fluid_vs_joint/` 下
