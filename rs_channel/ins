# RS 代码仓库重构指令

## 目标
将 RouterSense/RS 重构为四层清晰结构：`src/` | `experiments/` | `scripts/` | `deploy/`
另保留 `channel/` 用于远端通信（仅 pull，不 push）。

## 目标结构

```
RS/
├── src/routesense/
│   ├── __init__.py
│   ├── trace/                        ← 路由 trace 采集（保留不动）
│   │   ├── __init__.py
│   │   └── olmoe_router_trace.py
│   ├── topology/                     ← 网络拓扑（保留不动）
│   │   ├── __init__.py
│   │   ├── inventory.py
│   │   └── paths.py
│   ├── scheduler/                    ← 调度算法（核心）
│   │   ├── __init__.py
│   │   ├── oracle.py                 ← CP-SAT oracle（从 poc_line1.py 拆出）
│   │   ├── greedy.py                 ← LPT greedy（从 poc_line1.py 拆出）
│   │   └── fast.py                   ← 快速调度算法（待开发，先建空壳）
│   ├── evaluation/                   ← 评估与分析
│   │   ├── __init__.py
│   │   ├── cross_layer.py            ← Gate 1 跨层预测分析（从 poc_line1.py 拆出）
│   │   ├── traffic_matrix.py         ← 流量矩阵构建（从 poc_line1.py 拆出）
│   │   └── artifacts.py              ← artifact 写入（保留不动）
│   └── runtime/                      ← 分布式运行时（保留不动）
│       └── distributed_ep/
│           ├── core/
│           └── adapter/
│
├── experiments/                      ← 实验入口（一键运行，参数硬编码）
│   ├── poc_line1/
│   │   ├── exp_trace.py              ← 采集 trace
│   │   ├── exp_cross_layer.py        ← 跨层预测分析
│   │   ├── exp_oracle.py             ← oracle vs greedy 对比
│   │   └── exp_pairwise.py           ← pairwise 调度分析
│   ├── distributed/
│   │   ├── exp_nccl_smoke.py
│   │   └── exp_olmoe_ep.py
│   └── ablation/
│       └── configs/                  ← 消融实验配置 yaml
│
├── analysis/                         ← 分析工具（处理实验产出）
│   ├── __init__.py
│   ├── plot_makespan.py              ← makespan 对比可视化
│   ├── plot_prediction.py            ← 预测质量分析
│   ├── summarize_pairwise.py         ← pairwise 结果汇总
│   └── compare_strategies.py         ← 多策略对比
│
├── scripts/                          ← 脚手架/工具脚本
│   ├── verify_gpu_env.py
│   ├── verify_cluster_gpu_env.py
│   ├── verify_model_cache.py
│   ├── verify_model_parity.py
│   ├── probe_architecture.py
│   ├── router_trace_smoke.py
│   ├── single_gpu_smoke.py
│   ├── text_inference.py
│   ├── check_cluster_access.sh
│   ├── check_repo_parity.sh
│   ├── collect_logs.sh
│   ├── launch_remote.sh
│   ├── prefetch_model.sh
│   ├── prepare_node.sh
│   ├── prepare_2node_4gpu.sh
│   ├── stop_rs_jobs.sh
│   ├── sync_model_cache.sh
│   └── sync_repo.sh
│
├── deploy/                           ← 远端部署包
│   ├── inventory/                    ← 集群节点配置
│   ├── env/                          ← 环境依赖
│   └── README.md
│
├── channel/                          ← 远端通信目录（仅 pull，不 push）
│   ├── ins/                          ← 远端发来的指令
│   └── reply/                        ← 远端发来的回复
│
├── tests/
│   ├── test_poc_line1.py
│   └── test_structure_refactor.py
│
├── configs/                          ← 本地配置
│   ├── model/
│   ├── topology/
│   └── scheduler/
│
├── artifacts/                        ← 实验产出（gitignore）
├── pyproject.toml
└── README.md
```

## 执行步骤

### Step 1: 清理
1. 删除所有 `__pycache__/` 目录
2. 删除空目录：`src/routesense/oracle/`、`experiments/baseline/`、`experiments/paper/`、`experiments/stress/`
3. 删除 `src/routesense/runtime/local_test/`（单机验证已完成，archive 有备份）

### Step 2: 拆分 poc_line1.py
将 `src/routesense/evaluation/poc_line1.py`（1472行）拆分为：

- `src/routesense/scheduler/oracle.py`：
  - `pairwise_oracle()` 函数
  - `_pairwise_oracle_scipy()` 函数（fallback）
  - `PairwiseChunk` dataclass
  - 所有 CP-SAT / scipy 相关辅助函数

- `src/routesense/scheduler/greedy.py`：
  - `greedy_schedule_pairwise()`
  - `greedy_schedule_single_layer()`
  - `greedy_schedule_multi_layer()`

- `src/routesense/evaluation/cross_layer.py`：
  - `analyze_cross_layer_predictability()`
  - `analyze_cross_layer_correlation()`
  - `evaluate_gate2()`
  - `_decode_predicted_topk_by_sample()`
  - `GatePredictionStat` dataclass
  - `spearman_rank_correlation()` 及辅助统计函数（`_pearson`, `_rankdata`, `_summary_stats`）

- `src/routesense/evaluation/traffic_matrix.py`：
  - `build_sample_layer_matrices()`
  - `build_predicted_traffic()`
  - `combine_matrix_from_dispatch()`
  - `build_owner_by_expert()`
  - `build_same_prompt_batches()`
  - `TraceRecord` dataclass
  - `load_trace_jsonl()`
  - `_group_records_by_sample_token_layer()`

- `src/routesense/evaluation/analysis.py`：
  - `run_pairwise_analysis()`
  - `simulate_oracle_vs_greedy()`
  - `oracle_schedule_multi_layer()`
  - 其他编排/汇总函数

保留 `src/routesense/evaluation/artifacts.py` 不动。

### Step 3: 更新 __init__.py
- 新建 `src/routesense/scheduler/__init__.py`，导出所有调度相关函数
- 更新 `src/routesense/evaluation/__init__.py`，改为从新模块 import
- 确保所有 experiment 脚本的 import 路径正确

### Step 4: 整合 scripts/
1. 将 `deploy/scripts/` 下所有文件移到 `scripts/`
2. 将 `experiments/prerun/` 下所有文件移到 `scripts/`
3. 删除 `deploy/scripts/` 和 `experiments/prerun/`

### Step 5: 重写 experiments 入口
将 `experiments/poc_line1/` 下的 4 个脚本重写为统一格式：
- 文件名统一为 `exp_*.py`
- 参数硬编码在文件顶部常量区
- 每个文件一个 `main()` 函数，一键运行
- import 路径指向 `src/routesense/` 的新模块

同理处理 `experiments/distributed/scripts/` → 提升到 `experiments/distributed/`，去掉 scripts 子目录。

### Step 6: 创建 analysis/ 目录
新建 `analysis/` 目录，放入分析工具脚本。初期可以先创建 `__init__.py` 和占位文件，后续填充具体分析代码。

### Step 7: channel/ 目录
确保 `channel/ins/` 和 `channel/reply/` 目录存在（替代原 rs_channel/）。
添加 `.gitkeep` 保持目录结构。

### Step 8: 清理根目录 md 文件
根目录散落的 md 文件整理：
- `line2-thought.md` → 保留（当前讨论记录）
- `new-directions.md` → 保留（方向文档）
- `poc1-claim-risk.md` → 保留（审计提示词）
- `poc_line1_i_task.md` → 保留（任务书）
- `combine.md`, `perfer.md`, `infocom-direction-survey.md` → 移入 `docs/` 或评估是否可删除

### Step 9: 文件命名统一
- experiments 下：`exp_<功能>.py`
- scripts 下：功能描述性命名，snake_case
- analysis 下：`<动作>_<对象>.py`（如 plot_makespan.py）
- 所有 Python 文件 snake_case

### Step 10: 验证
1. 运行 `cd RS && pytest -q tests/` 确保所有测试通过
2. 检查所有 import 路径无报错
3. 运行一个 smoke 实验确认端到端可用

## 约束
- **不修改任何算法逻辑**，只做文件移动和 import 修正
- `channel/` 目录只做 pull，这边不做 push
- 保留 archive/ 和 legacy/ 不动
- 每步完成后 git add + commit，不要一次性大提交
