# Remote Entrypoint

`deploy/remote/` only contains thin wrappers for launching formal experiment
modules on a prepared node. Runtime logic remains in `src/rs/` and experiment
definitions remain in `experiments/`.
