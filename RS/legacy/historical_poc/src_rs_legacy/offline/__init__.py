from __future__ import annotations

"""Offline-only research utilities.

These modules may consume oracle traces, full-sequence single-GPU router
observations, and calibrated counterfactual models. They must not be treated
as deployable online runtime measurements.
"""
