# Archive Configs

This directory is reserved for historical configs that are no longer part of the
official entrypoint surface. Current official configs live under `configs/official/`
and reusable sub-components live under `configs/components/`.
