# Dialogue Evidence

This directory contains only the latest handoff evidence for external review.
Historical rounds should be inspected through Git commits.
Each round deletes and overwrites this directory.
Do not store models, full outputs, raw traces, profiler dumps, large JSONL timelines, caches, or old archives here.
