{
  "status": "M5_EXPERIMENT_REPRODUCIBILITY_READY",
  "completed_with_evidence": [
    "ExperimentSpec / SuiteSpec / PlanningCase / RunPlan contracts",
    "schema_version=2 ExperimentConfigLoader",
    "lossy schema v1 migration replaced with explicit UnsupportedLegacyExperimentConfig failure",
    "resolved_config.yaml and migration_report.json emission",
    "single CLI entrypoint python -m rs.experiments.cli",
    "official schema v2 config set with preserved model/topology/workload/runtime/evaluation semantics",
    "default pytest collect-only succeeds without OR-Tools import-time failure",
    "OfflineEvaluationRunner invokes formal M4 services and returns non-placeholder typed ResultBundle",
    "GlooFunctionalRunner invokes formal M123 gate through subprocess and records typed gate artifacts",
    "CLI run writes reproducible per-run artifacts and emits pure JSON on stdout",
    "committed-tree source archives resolve commit identity without .git via handoff manifest fallback"
  ],
  "implemented_not_verified": [
    "GPU and multinode runners remain environment-scoped and may return environment_not_run"
  ],
  "hard_blockers": [],
  "environment_not_run": [
    "GPU evidence",
    "multinode evidence",
    "OR-Tools exact oracle evidence"
  ],
  "deferred_cleanup": [
    "legacy core.experiment_config remains in tree for compatibility and should be retired after migration",
    "legacy v1 configs are archived but still present for historical reference"
  ],
  "test_commands": [
    "PYTHONPATH=.;src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q .\\tests\\test_experiments_cli_v2.py",
    "PYTHONPATH=.;src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest --collect-only -q",
    "PYTHONPATH=.;src python -m rs.experiments.cli run --config .\\configs\\official\\offline_evaluation.yaml --output-dir .\\outputs\\r4_offline_cli",
    "PYTHONPATH=.;src python -m rs.experiments.cli run --config .\\configs\\official\\gloo_functional.yaml --output-dir .\\outputs\\r4_gloo_cli"
  ]
}
