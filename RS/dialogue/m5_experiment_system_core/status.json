{
  "status": "M5_EXPERIMENT_SYSTEM_CORE_BLOCKED",
  "completed_with_evidence": [
    "ExperimentSpec / SuiteSpec / PlanningCase / RunPlan contracts",
    "schema_version=2 ExperimentConfigLoader",
    "lossy schema v1 migration replaced with explicit UnsupportedLegacyExperimentConfig failure",
    "resolved_config.yaml and migration_report.json emission",
    "single CLI entrypoint python -m rs.experiments.cli",
    "RunnerRegistry for Phase A run kinds",
    "official schema v2 config set with preserved model/topology/workload/runtime/evaluation semantics",
    "default pytest collect-only succeeds without OR-Tools import-time failure"
  ],
  "implemented_not_verified": [
    "real service-backed runner execution beyond Phase A stubs"
  ],
  "hard_blockers": [
    "OfflineEvaluationRunner, GlooFunctionalRunner, and DiagnosticRunner still return explicit placeholder ResultBundle states instead of invoking formal M4/M123 services"
  ],
  "environment_not_run": [
    "GPU, multinode, and OR-Tools-backed suites"
  ],
  "deferred_cleanup": [
    "legacy core.experiment_config remains in tree for compatibility and should be retired after migration",
    "Phase A runners currently return explicit non-claiming ResultBundle placeholders until M2/M4 services are wired"
  ],
  "test_commands": [
    "python -m compileall RS/src RS/tests",
    "python -m pytest --collect-only -q",
    "PYTHONPATH=RS/src;RS;. python -m pytest -q RS/tests/test_experiments_cli_v2.py"
  ]
}
