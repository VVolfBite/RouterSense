{
  "status": "M123_PARALLEL_INTEGRATION_READY",
  "completed_with_evidence": [
    "host/runtime now build CanonicalPlanPublisher, selectable RuntimeExecutionPipeline, and RuntimeInstrumentation on the formal path",
    "phase_sync backend integrated publication -> P0 -> P1 gate passes",
    "async_release backend integrated publication -> P0 -> P1 gate passes",
    "local release ledger semantics enforce per-rank P0 and P1 release ownership",
    "missing actual execution outcomes now fail closed during result finalization",
    "off instrumentation produces zero measurement events on the formal runtime path",
    "debug instrumentation preserves runtime execution while remaining non-performance-claimable",
    "focused runtime integration and execution tests pass on the integration branch"
  ],
  "implemented_not_verified": [
    "GPU/NCCL integration remains out of scope for this CPU-Gloo closure"
  ],
  "hard_blockers": [],
  "environment_not_run": [
    "GPU evidence",
    "multinode evidence"
  ],
  "deferred_cleanup": [
    "legacy root dialogue files remain noisy",
    "compatibility runtime modules are still present for non-formal paths"
  ],
  "test_commands": [
    "PYTHONPATH=RS/src;RS;. PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q RS/tests/unit/test_m123_runtime_integration.py RS/tests/unit/test_execution_api.py RS/tests/test_architecture_dependencies.py",
    "PYTHONPATH=RS/src;RS;. python -c \"from experiments.distributed.run_m123_integrated_publication_execution_gloo import run_gate_with_backend; import json; print(json.dumps(run_gate_with_backend(instrumentation_mode='perf_light', execution_backend='phase_sync'), sort_keys=True))\"",
    "PYTHONPATH=RS/src;RS;. python -c \"from experiments.distributed.run_m123_integrated_publication_execution_gloo import run_gate_with_backend; import json; print(json.dumps(run_gate_with_backend(instrumentation_mode='perf_light', execution_backend='async_release'), sort_keys=True))\"",
    "PYTHONPATH=RS/src;RS;. python -c \"from experiments.distributed.run_m123_integrated_publication_execution_gloo import run_gate_with_backend; import json; print(json.dumps({'status': run_gate_with_backend(instrumentation_mode='off', execution_backend='phase_sync')['status']}, sort_keys=True))\"",
    "PYTHONPATH=RS/src;RS;. python -c \"from experiments.distributed.run_m123_integrated_publication_execution_gloo import run_gate_with_backend; import json; print(json.dumps({'status': run_gate_with_backend(instrumentation_mode='debug', execution_backend='phase_sync')['status']}, sort_keys=True))\""
  ]
}
