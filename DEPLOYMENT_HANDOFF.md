# Retired deployment handoff

The authoritative execution task is now [`task-test-deploy.md`](task-test-deploy.md).
This redirect is retained only for compatibility with older source packages.
Do not use historical deployment instructions.
