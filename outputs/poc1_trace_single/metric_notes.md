# Metric Notes

This directory is for one real trace sample. Inspect `one_sample_trace.json` for router logits/probabilities, selected experts, gate weights, entropy, and top1-top2 gap. Inspect `batch_routing_summary.json` for active destinations and bucket summaries.
