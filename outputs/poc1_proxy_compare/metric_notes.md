# Metric Notes

This directory contains proxy comparison and critical bucket proxy outputs. For ranking divergence, inspect `proxy_comparison_summary.json`. For decision-level validation, inspect `critical_bucket_proxy_summary.json`, especially v2 top-1 hit rates, mean rank of critical top-1, per-layer results, and grouping summaries.
