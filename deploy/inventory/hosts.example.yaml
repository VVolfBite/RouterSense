# Fill only the values shown below. Stable RouterSense paths, ranks and ports
# are derived automatically.
nodes:
  - host: NODE0_INTERNAL_IP
    ssh_host: NODE0_SSH_HOST
    gpu_count: 2
    model_path: /MOUNTED/CLOUD/MODEL/PATH
  - host: NODE1_INTERNAL_IP
    ssh_host: NODE1_SSH_HOST
    gpu_count: 2
    model_path: /MOUNTED/CLOUD/MODEL/PATH
